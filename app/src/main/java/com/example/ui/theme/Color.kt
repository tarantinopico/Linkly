package com.example.ui.theme

import androidx.compose.ui.graphics.Brush
import androidx.compose.ui.graphics.Color

val PremiumBackgroundTop = Color(0xFF15161C)
val PremiumBackgroundBottom = Color(0xFF0E0F13)

val CardSurfaceDark = Color(0xFF1A1C24)
val CardSurfaceLight = Color(0xFF20222C)
val CardBorderLight = Color(0x14FFFFFF) // 8% white for the edge

val TextPrimary = Color(0xFFF0F0F2)
val TextSecondary = Color(0xFF9A9A9F)
val TextAccent = Color(0xFFFFFFFF)

// Gradients
val AccentGradientStart = Color(0xFF7C5CFF)
val AccentGradientEnd = Color(0xFF4D8DFF)
val AccentGradient = Brush.linearGradient(listOf(AccentGradientStart, AccentGradientEnd))

val FavoriteGradientStart = Color(0xFFFF5C8D)
val FavoriteGradientEnd = Color(0xFFFF4D4D)
val FavoriteGradient = Brush.linearGradient(listOf(FavoriteGradientStart, FavoriteGradientEnd))

val UnreadGradientStart = Color(0xFF00C6FF)
val UnreadGradientEnd = Color(0xFF0072FF)
val UnreadGradient = Brush.linearGradient(listOf(UnreadGradientStart, UnreadGradientEnd))

val CategoryGradientStart = Color(0xFFB06AB3)
val CategoryGradientEnd = Color(0xFF4568DC)
val CategoryGradient = Brush.linearGradient(listOf(CategoryGradientStart, CategoryGradientEnd))

val AllGradientStart = Color(0xFF11998E)
val AllGradientEnd = Color(0xFF38EF7D)
val AllGradient = Brush.linearGradient(listOf(AllGradientStart, AllGradientEnd))

// Colors for Theme Scheme
val MutedPurple = Color(0xFF7C5CFF)
val MutedPurpleDark = Color(0xFF4D8DFF)
val CharcoalBackground = Color(0xFF0E0F13)
val CardBackground = Color(0xFF1A1C24)
val CardBackgroundSecondary = Color(0xFF20222C)
val CardBorder = Color(0xFF2D2F3A)
